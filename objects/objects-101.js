// Challenge

// name, age, location

let myPerson = {
    name: 'Caleb',
    age: 25,
    location: 'Orem'
}

// eg Andrew is 27 and lives in Philadephia

console.log(`${myPerson.name} is ${myPerson.age} years old and lives in ${myPerson.location}.`)

// increase age by 1 and print message

myPerson.age = myPerson.age +1

console.log(`${myPerson.name} is ${myPerson.age} years old and lives in ${myPerson.location}.`)

